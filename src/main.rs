fn main() {
    println!("Hello, world!");

    let array = vec![12, 4, 2, 7, 9, 27, 1, 3, 8, 6, 5, 0];
    let number = 7;
    bigO(array, number);
}

fn bigO(arrayOfInts: Vec<u32>, numberToSearch: u32) {
    let mut idx_start = 0;
    let mut idx_end = arrayOfInts.len();

    while(idx_start != idx_end){
        
    }
}
